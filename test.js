// let numbers = [1,1,0,2]
let max = Math.max(1,2,4,6)
max

const times = {
    one: 0,
    two: 1,
    three: 2,
    four: 0,
}

let most = Math.max(times.one, times.two, times.three, times.four)
most;